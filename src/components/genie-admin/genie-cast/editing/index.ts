/**
 * Editing UI Components — Universal Video/Audio/Podcast Production
 *
 * Style-agnostic editing system supporting:
 *   - 93 production styles (master + extended)
 *   - All formats: video, podcast, webcast, presentation, animation, avatar
 *   - Offline video imports
 *   - Multi-track timeline (video, audio, subtitle, overlay)
 *   - 30+ platform export targets
 *   - Scene-aware teleprompter with narration + visual script editing
 */

export { VideoTimelineEditor } from './VideoTimelineEditor';
export { ExportDistributionPanel } from './ExportDistributionPanel';
export { SceneAwareTeleprompter } from './SceneAwareTeleprompter';
export { VideoEffectsPanel } from './VideoEffectsPanel';
