/**
 * CONFIGURABLE GENIE PAGE
 * Main page for managing brand-specific Genie configurations
 */
import React from 'react';
import { BrandConfigurationManager } from '@/components/configurable-genie/BrandConfigurationManager';

export const ConfigurableGeniePage: React.FC = () => {
  return <BrandConfigurationManager />;
};