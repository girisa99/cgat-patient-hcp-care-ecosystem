# PostgreSQL connection string in URL form (preferred)
DATABASE_URL=postgresql://user:password@localhost:5432/mydatabase

# -- OR -- supply individual parameters instead of DATABASE_URL
PGHOST=localhost
PGPORT=5432
PGUSER=user
PGPASSWORD=password
PGDATABASE=mydatabase

VITE_SUPABASE_URL=https://project.supabase.co
VITE_SUPABASE_ANON_KEY=public-anon-key
NGROK_AUTHTOKEN=your-ngrok-token
# Base URL used by Playwright tests (defaults to http://localhost:5173)
E2E_BASE_URL=https://dev.geniecellgene.com